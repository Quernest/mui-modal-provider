name: CI

on: [push]

jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: borales/actions-yarn@v2.3.0
        with:
          cmd: install
      - run: yarn
      - run: yarn test
